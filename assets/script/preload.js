const preload = document.getElementById("preload");

document.addEventListener("DOMContentLoaded", function() {
    //setTimeout(() => { preload.classList.add("loaded"); }, 2000);
    preload.classList.add("loaded");
});